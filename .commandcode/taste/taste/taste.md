# Taste
- Development environment is Windows with Laragon, PHP 8.3, and MySQL 8.4. Confidence: 0.9
- Laragon serves the project root as document root, not `public/` — pages are reached via `http://localhost/kasir-minimarket/public/...`; URLs without the `public/` segment return 404, so a "can't login / page error" report (e.g., transaksi.php) may be a wrong URL path rather than a code bug. The permanent fix is a root `.htaccess` that path-preserving rewrites every request to `public/`, so short URLs (`/transaksi.php`, `/member_login.php`, `/api.php`) work identically to `/public/...` — when a page "errors", verify the HTTP status from the user's URL first, before debugging code. Confidence: 0.95
- On this Windows cmd, inline `php -r` snippets with raw backslash namespace instantiation (`new \App\...`) break due to escaping — use a small scratchpad PHP script file instead of `php -r` for one-off DB checks. Refinement: a double-quoted `php -r` snippet DOES work when namespaces are imported via `use` statements and models are invoked through `fn()` arrow-function closures (e.g., looping `[[label, fn() => (new Model)->getDataTabel(...)], ...]` over many models) — successfully used to verify a systemic search fix across 9 models in a single command, so multi-model verification loops don't always need a scratchpad file. Confidence: 0.8
- Windows cmd does not support bash-style heredocs (e.g., `git commit -F - <<'EOF'` fails outright) — use `git commit -m` with multiple `-m` arguments for multi-paragraph commit messages instead. Confidence: 0.85
- Windows cmd lacks GNU grep options (`grep -oP`) and inline variable assignment/command substitution — for CSRF-token extraction and HTTP page verification after login, use PowerShell (`Invoke-WebRequest` with a WebRequestSession + `[regex]::Match`) instead of chained curl/grep one-liners. Confidence: 0.8
- The project's PDO connection runs with `ATTR_EMULATE_PREPARES=false`, so a named parameter cannot be reused twice in one prepared statement — when the same value is needed more than once (e.g., atomic stock `UPDATE ... SET stok = stok - :qty WHERE ... AND stok >= :qty_cek`), bind it under distinct names or PDO throws `HY093 Invalid parameter number`. Confidence: 0.9
- Global helper functions defined in `src/autoload.php` must be wrapped in `if (!function_exists(...))` because `database/seed.php` is `require`'d again at the end of `test/e2e.php` (auto-restore), re-including autoload — unguarded declarations cause a "Cannot redeclare function" fatal that aborts the seed mid-run and leaves the `users` table empty. Confidence: 0.8
- MySQL runs on 127.0.0.1:3306 with user `root` and no password. Confidence: 0.9
- Write all PHP code using PHP 8.3 standards (not 8.2 or earlier). Confidence: 0.9
- Communicates in Indonesian (Bahasa Indonesia); expects replies in the same language. Confidence: 0.9
- Gives terse, informal feature directives (e.g., "yaudahh dibikin otomatis lahhh harga beli nya sesuai dengan supplier") and expects the assistant to autonomously design and implement the full stack (model method + API endpoint + frontend JS) without clarifying questions — same autonomy as with bug reports. Confidence: 0.6
- Explicitly delegates feature/UI-upgrade work to run with zero approval-seeking — "tolong kerjakan fitur baru atau upgrade ui/ux. tanpa bertanya tau minta persetujuan aku" (user was busy and didn't want to be interrupted); the assistant is expected to pick the highest-impact feature itself, implement it end-to-end (migration → model → form → UI → verification), and treat that as approved; this explicitly overrides the usual plan-summary/approval step whenever the user says not to ask. Confidence: 0.8
- Wants derived form fields auto-filled from related records instead of manual entry — asked to make "harga beli" auto-populate from the selected supplier's latest purchase history; the accepted behavior fetches the last purchase price for that product+supplier with sensible fallbacks (any supplier, then stored product price) and leaves the field still manually editable. Confidence: 0.6
- Asks plain-language "what does this field/feature mean?" questions about unfamiliar UI elements and expects a simple, real-world explanation (the flow, what it scans/needs, what happens on mismatch, or the business meaning of form fields) rather than technical detail — e.g., asked "kalo scan barcode kamera itu nge scan apaa" (explained as packaging barcodes matched against stored product data) and "di tambah produk harga sama harga beli apa bedanya?" (explained as selling price vs cost used for profit, auto-filled from Stok Masuk). Confidence: 0.7
- These "what does this field mean?" questions often turn into instant feature requests: after the harga beli explanation was accepted, the user immediately said "yaudahh dibikin otomatis lahhh" — expect the explanation of a field's purpose to be followed by a directive to automate/simplify it. Confidence: 0.5
- Communicates page requirements as concise wireframe-style specs (layout sections + behavior) and expects the implementation to follow them. Confidence: 0.7
- Prefers front-end assets (Bootstrap, Bootstrap Icons + fonts, jQuery, DataTables incl. id.json, Chart.js) vendored locally under `public/assets/vendor/` with no CDN references in pages — the user's network/ISP blocks jsdelivr (HTTP 403), which made pages render as blank white screens even though the server returned 200 with full HTML; the accepted fix downloaded every CDN asset locally and rewrote all public pages to local paths so the app no longer depends on internet access. Confidence: 0.8
- Prefers new UI pages to reuse existing model methods (e.g., Transaksi::tambahItem, Kasir::prosesTransaksi, ReturBarang::prosesRetur) instead of reimplementing business logic — explicitly told to "ikuti alur di ReturBarang::prosesRetur() yang sudah ada". Confidence: 0.8
- Prefers multi-step database writes to be atomic: wrap stock updates + record inserts in one DB transaction with automatic rollback if anything fails mid-way. Confidence: 0.8
- Wants each new feature page wired into the admin navigation (e.g., add menu buttons in admin.php for new pages like Supplier/Retur). Confidence: 0.7
- Works on projects incrementally across sessions — pauses mid-task (sometimes accidentally, e.g., hitting ESC, then "lanjutkan, maaf kepencet esc") and expects the assistant to resume the halted step and re-orient itself from the project state without re-asking for requirements (e.g., returning with a bare "lanjut" and letting the assistant pick the next pending item from the plan on its own) — reinforced again by a bare "lanjutkan progres nya" that carried no scope, leaving the assistant to orient from git state + roadmap and present a next-step plan). Confidence: 0.98
- Prefers authentication with native PHP sessions (store user_id/role in $_SESSION, role-based redirect after login) rather than a framework or library-based auth. Confidence: 0.9
- Wants page code to take the current user's identity from $_SESSION rather than hardcoding IDs/constants (e.g., replace hardcoded KASIR_ID with the session user id). Confidence: 0.9
- Prefers passing values into functions as explicit parameters (e.g., `aksiBayar(..., int $kasirId, string $namaUser)`) over relying on global-scope variables/constants inside functions — approved a fix that changed function signatures instead of using `global`/constants. Confidence: 0.6
- Distinguishes "not logged in" (redirect to login) from "logged in but wrong role" (show an HTTP 403 access-denied page with a friendly message like "Anda tidak memiliki akses ke halaman ini") instead of redirecting unauthorized users to login — explicitly requested this for laporan.php. Confidence: 0.7
- Prefers pragmatic, minimal implementations over complex ones when constrained (no external library / limited time) — explicitly allowed exporting to CSV instead of a real PDF ("minimal ekspor ke CSV kalau PDF terlalu kompleks"). Confidence: 0.7
- Wants UI styling to be consistent across all pages — when a page was left out of the applied theme (index.php), asked to bring it in line with the same navbar/card/bootstrap-icons styling used on the other pages ("samakan aja dlu index nya"). Confidence: 0.7
- Wants transactional documents/output (e.g., struk receipt) to be very detailed: include amount paid (dibayar) and change (kembalian) plus item-level breakdown (unit price × qty, per-item subtotal, discount, payment method, cashier), not just the grand total — explicitly asked "pokoknya harus detail banget ya". Confidence: 0.7
- App timestamps must be in Indonesian local time (WIB / Asia/Jakarta) — reported the admin report page showing 16.35 when the actual local time was 23.35 as a bug; expects displayed times to always match the local clock, so timezone is set globally (date_default_timezone_set) and any legacy data stored in UTC gets shifted +7 hours. Confidence: 0.8
- Wants an implementation plan summary presented for review before coding starts — explicitly asked "Tampilkan dulu ringkasan rencana implementasi ke aku sebelum mulai coding" before the cashier-management feature was implemented. Confidence: 0.9
- Follows existing project patterns instead of inventing new architecture — explicitly required "Ikuti pola yang SUDAH ADA di project ini (jangan bikin arsitektur baru)": reuse the CRUD model pattern (Kategori/Produk), the POST action routing pattern (switch $_POST['aksi'] in admin.php), and the existing theme.css. Confidence: 0.9
- CRUD methods for a table belong in the model class that represents that entity (User.php owns CRUD for the users table, just as Produk.php owns the produk table), while Admin-level "kelolaX()" methods stay as empty contracts like kelolaProduk()/lihatLaporan() — explicitly corrected a plan that placed cashier CRUD methods in Admin.php, asking them moved to User.php. Confidence: 0.9
- Wants constraint-violation errors (e.g., deleting a cashier who still has transactions, FK ON DELETE RESTRICT) rejected with a clear user-facing error message in the same style as existing pages — never let raw SQL errors surface to the user. Confidence: 0.7
- For large open-ended upgrade requests, prefers fixing existing bugs before layering new features on top — chose "perbaiki bug dulu" (payment-shortfall validation, cashier name in reports, discount codes) as the safe foundation before building new features like the dashboard. Confidence: 0.6
- Prefers a clean minimalist SaaS design language (light background, white card surfaces, thin borders, subtle shadows, larger bolder typography, flat buttons) over dark/neon or heavy-glassmorphism themes — picked "minimalis SaaS" for the visual upgrade while keeping the existing teal-emerald + amber brand palette. Confidence: 0.6
- Wants navigation/layout to stay tidy and never overflow — reported the admin navbar as broken/messy ("beserak gak beraturan kyk di serang hacker") when too many menu items wrapped into irregular rows; expects the navbar to remain a single clean row (fixed with `container-fluid` + `flex-lg-nowrap` and grouping secondary menus into a "Lainnya" dropdown). Confidence: 0.6
- Wants an admin analytics dashboard as the command center: today's sales total, transaction count, items sold, average per transaction, a 7-day sales chart, top products, low-stock warnings, latest transactions, and payment-method breakdown. Confidence: 0.6
- Wants the core cashier transaction flow (adding items, applying discount, processing payment, closing receipt) to work without reloading the page — explicitly requested "transaksi dapat di input dan diproses tanpa harus memuat ulang halaman"; the implemented AJAX/fetch approach (server returns JSON fragments, non-AJAX fallback kept) was accepted. Confidence: 0.7
- Wants backend business logic architected with explicit OOP design patterns rather than rigid IF/ELSE dispatch — explicitly requested the Strategy Pattern for payment handling (interface `PaymentMethod` with `prosesBayar($total, $jumlahBayar)`, strict polymorphic implementations in `PembayaranTunai`/`PembayaranNonTunai`, and removal of hardcoded payment-type conditionals from `Transaksi`). Confidence: 0.9
- Wants post-transaction side-effects (receipt JSON, sales recap) decoupled from the core flow via the Observer Pattern — explicitly requested `Observer` (update(Subject $subject)) and `Subject` (attach/detach/notify) interfaces, `Transaksi implements Subject` calling `notify()` after successful payment, `Struk` preparing the JSON receipt output, `LaporanPenjualan` recording the recap to DB, with no hardcoded Struk/Laporan logic inside Transaksi. Confidence: 0.9
- Provides precise architecture AND UI specs — exact interface names, method signatures, class assignments, layout percentages, and behavior (e.g., `Observer::update(Subject $subject)`; abstract `User` with `protected` props and abstract `getHakAkses()`, `Admin`/`Kasir extends User` overriding it, polymorphic login by DB role; UI: CSS variables `:root` light / `.dark-mode` dark, kiosk split-screen 70/30, localStorage theme key) — and expects the implementation to match the spec exactly. Confidence: 0.8
- Prefers Dependency Injection for collaborating objects — explicitly asked `Transaksi` to receive its payment strategy (a PaymentMethod object) via constructor or setter instead of branching on payment type internally. Confidence: 0.8
- Demands strict layering in PHP: View files (public/*.php) must contain NO raw SQL, procedural logic, or direct DB access — Views are pure clients that only consume JSON; all AJAX requests (Chart.js, DataTables, etc.) must route through a PBO Controller that returns pure JSON via domain objects. Explicitly: "DILARANG KERAS ada query SQL mentah, logika prosedural, atau akses basis data langsung di dalam file Views". Confidence: 0.9
- Wants full encapsulation of domain data: private properties with getters/setters for all nominal/amount data, with validation at the setter level (e.g., `setJumlah()` clamping to non-negative) — reaffirmed with "Pertahankan enkapsulasi properti secara penuh" on the dashboard/reporting upgrade. Confidence: 0.9
- Extends the OOP-pattern requirement to reporting/analytics: wants chart & table data supplied via interface-driven polymorphism — define an interface (e.g., `DataReporter` with abstract `getAgregasiGrafik()` and `getDataTabel()`), force domain classes (`LaporanPenjualan`, inventory manager) to implement it, and have controllers call those methods for pure JSON output. Confidence: 0.9
- Once the Controller + DataReporter + server-side DataTables architecture is established, wants ALL admin/list pages converted to it for consistency — the user's bare "lanjut" approvals drove the conversion of `admin.php`'s product table (`produk.tabel` endpoint, `InventarisController`) and then `supplier.php`'s supplier table (`supplier.tabel` endpoint on the same controller); server-only concerns (Edit/Hapus action buttons) are rendered client-side via JS render callbacks while the table data comes from the API; the conversion applies to data/list tables only — pure CRUD form pages (diskon, user/kasir) may stay server-rendered. Confidence: 0.8
- Prefers Dependency Injection for collaborating objects — explicitly asked `Transaksi` to receive its payment strategy (a PaymentMethod object) via constructor or setter instead of branching on payment type internally; the same DI principle extends to new flows (controllers call methods on injected DataReporter objects). Confidence: 0.9
- Wants the theme architecture built as a Global Design System using CSS variables — light-mode tokens in `:root`, dark-mode overrides in `.dark-mode` — with a Light/Dark toggle button in the main navbar (all pages) that persists the preference to localStorage, so admin and kasir pages stay 100% visually synchronized. Confidence: 0.9
- Wants the POS/cashier page as a dedicated Kiosk Mode: main navbar/sidebar hidden, 70/30 split-screen layout (dynamic cart table on the left; numpad, summary, and payment actions on the right); the kiosk must ALWAYS render in the light/white theme — explicitly asked "layar kasir dibikin putih aja lah", so theme.js forces light on kiosk pages and ignores any saved dark preference (other admin pages keep their light/dark toggle). Confidence: 0.85
- Considers it "Sangat Penting" that the POS panel always shows the active cashier's name and a Logout button at the top of the right panel, so a cashier can never get stuck without a way to exit the system. Confidence: 0.9
- Wants numeric/amount elements (totals, prices, change, payment input) rendered in an extra-bold monospace font with tabular numerals (a `.font-num` utility), distinct from the base UI font. Confidence: 0.8
- Prefers using **n8n** (external automation/workflow platform) for external service integrations (e.g., WhatsApp notifications per transaction) rather than implementing HTTP client calls or cURL logic directly in PHP — explicitly proposed n8n as the integration mechanism, consistent with the no-external-PHP-dependencies stance; the PHP side's job is to produce structured data (e.g., via the Observer pattern's outbox), then delegate the actual external API call to an n8n workflow/webhook. Confidence: 0.6
- When overhauling an existing page's layout (e.g., the POS kiosk redesign), existing interactions — AJAX add-item/payment flows and keyboard shortcuts — must keep working exactly as before; layout changes must not break behavior. Confidence: 0.8
- Wants front-end tables localized in Indonesian — DataTables initialized with `language: { url: 'https://cdn.datatables.net/plug-ins/2.1.8/i18n/id.json' }` so pagination/search strings are in Bahasa Indonesia. Confidence: 0.6
- Includes the DataTables Bootstrap5 CSS (`dataTables.bootstrap5.min.css`) via CDN when adding server-side DataTables to a page, matching the DataTables + Bootstrap JS includes already present. Confidence: 0.7
- Expects JSON API responses to be pure JSON — any PHP warning leaking into the output (e.g., "Array to string conversion" because DataTables sends `search[value]` as an array) breaks the client (DataTables "Invalid JSON response" pop-up) and is a bug to fix; server-side DataTables params should be normalized at the API entry point (`search[value]` array → string, `start`/`length`/`draw` → int). Confidence: 0.7
- Specifies exact hardware/browser technology stacks in feature requirements — named WebUSB/WebSerial/WebBluetooth APIs and the ESC/POS protocol as the required integration approach, and mandated that thermal receipt printing happen fully in the background (raw bytes, no browser print dialog at all) and that scale weights be read automatically (cashier must not type weights manually or click the browser print button). Confidence: 0.7
- Wants hardware/API integrations to be additive with graceful fallback — the approved plan kept the full manual input flow working when Web Serial/browser APIs or physical devices are unavailable ("mode kompatibilitas"), so the existing kiosk flow must never break without hardware; the camera barcode scan feature followed the same pattern (manual barcode input and USB-scanner flow untouched, camera stream auto-stopped on modal close, clear error message when the camera is denied/unsupported). Confidence: 0.7
- Fixes PHP scope bugs by adding the missing `global` declaration inside a view helper function (added `global $namaUser, $shiftAktif;` to `renderFragmentKananKiosk()`) rather than refactoring signatures — a pragmatic contrast to the earlier explicit-parameters preference; the accepted fix was the smallest change that eliminated the `Undefined variable` warning. Confidence: 0.55
- When e2e green-light is followed by a manual HTTP verification pass, the workflow is: start the PHP dev server → curl login (cookie jar + CSRF) → hit each new page asserting 200 → hit each JSON API endpoint → then exercise the full POST flow (buka kas) and assert UI state changed (kiosk-kiri no longer `d-none`, Tutup Kas button present) — then clean up: close test shifts via a scratchpad script, kill the server, delete temp files. Confidence: 0.6
- The app is accessed over plain HTTP with a non-localhost hostname (Laragon *.test domain / LAN IP, not `http://localhost`), where browsers make `navigator.mediaDevices` unavailable — so camera-dependent features must detect this cause and show a diagnostic error telling the user to open via `http://localhost/...` or HTTPS, rather than only a generic "browser unsupported" message. Confidence: 0.6
- Extends the browser-API integration to barcode scanning: asked whether the POS barcode scan could "bener bener buka kamera" — i.e., use the device camera via native browser APIs (getUserMedia + BarcodeDetector, with a small JS-library fallback like html5-qrcode/ZXing for unsupported browsers) so scanning behaves like a real scanner without extra hardware; the proposed UX (a "Scan Kamera" button beside the manual barcode input, small video preview that auto-closes once a barcode is read, manual input kept as fallback) fits the established additive-with-fallback pattern. Confidence: 0.6
- Expects API endpoints used at the POS counter to be accessible to the kasir role, not admin-only — when the hardware config endpoint returned 403 for a logged-in kasir, the role guard was adjusted so `hardware.config` (needed by the cashier for scales/printer) is allowed for kasir while the rest stays admin-only. Confidence: 0.5
- Likes to have the project reviewed from the perspective of a demanding external client or competition judge (e.g., "bertindak jadi klien dari perusahaan besar seperti mall plaza") before upgrades: the review should assess implemented features & design strengths, list the real-world gaps a client/jury would ask about (barcode scanning, stock-in/purchasing, profit/loss reporting, store settings, member loyalty, PDF export, soft-delete, product photos), and propose a concrete page inventory plus design polish to make the app competition-winning — the review then drives the upgrade roadmap. Confidence: 0.7
- Expects a full QA audit/debug pass ("evaluasi total ... saya rasa sebagai QA masih kureng") to treat money accuracy and security as first-class, not optional: money bugs were fixed first (double-applied discount, tax excluded from stored totals/payment validation, recap written after DB commit, float rounding, non-historical HPP), then security (stored XSS via AJAX innerHTML, missing CSRF on every POST, raw SQL errors leaking to users, no login rate-limit, stock race conditions), then UX/edge cases (gram-product UI missing, reversed date ranges, kiosk not responsive, barcode refocus) — each verified with e2e + curl. Confidence: 0.7
- Treats critical security findings as blockers that must be fixed immediately before any feature work — explicitly ranked debug files leaking phpinfo(), the whole project being web-servable (config/.git/test/database/vendor exposed), and upload validation trusting the browser's Content-Type as "Kritis — perbaiki SEKARANG sebelum lanjut fitur apapun"; security remediation outranks feature work and comes with prescribed fixes the assistant is expected to implement in full. Confidence: 0.9
- Wants the deploy architecture to keep public/ as the ONLY web-servable directory — the correct setup is document root pointing at public/; the root .htaccess redirect used for local Laragon convenience is only a band-aid, not a substitute; internal folders (config, database, src, test, vendor, .git, .commandcode) and files (composer.json, .env) must be explicitly blocked (403) even if the root is ever served directly, and each blocked/working path should be verified over HTTP. Confidence: 0.9
- Wants upload validation done server-side from the actual file content, never from the browser-claimed Content-Type: `getimagesize()` on the tmp file must confirm it is a real image and the stored extension must come from the detected MIME, plus a second layer of defense (uploads/.htaccess with `php_flag engine off` and script-file denial) so a slipped-in PHP file can never execute. Confidence: 0.9
- Treats unused dependencies as liabilities (repo bloat + attack surface) — dompdf sitting in composer.json/vendor while unused by any code was flagged because "dependency nganggur nambah ukuran repo & permukaan serangan"; a dependency that's present should either be put to real use (the already-vendored Dompdf was used to implement genuine PDF export) or removed, and dead dependencies shouldn't linger. Confidence: 0.65
- Prefers feature improvements delivered one feature at a time, never batched — explicitly asked "tolong di improve satu satu fitur nya, jangan sekaligus biar nggak ngerusak kualitas" (improve one by one so quality isn't damaged); each feature is completed, linted, and e2e-verified before starting the next. Confidence: 0.8
- Applies the same one-at-a-time discipline to debugging: debug features one by one, never all at once, and explicitly confirm after each feature is fixed before moving to the next — "debug satu persatu fitur nya jangan sekaligus. konfirmasi jika sudah selesai" (debug features one by one, not all at once; confirm when done). Confidence: 0.8
- Wants business workflows to be logically coherent with provenance-based validation — e.g., a return must go back to the supplier of the original stock-in (pembelian), and a product with no purchase history cannot be returned at all ("biar alur nya masuk akal"); the coherence rule is enforced in the model so the UI cannot bypass it. Confidence: 0.7
- Prefers system-generated unique identifiers over manual admin entry — explicitly questioned why admins must type member numbers themselves ("nomor membernya masa harus diisi sendiri sama adminnya"); the accepted design auto-generates member numbers (MEM-XXXXXX, uniqueness-guaranteed) so nothing is left for admin to invent. Confidence: 0.7
- Wants end-customers (members) to have their own self-service login accounts — members should log in themselves (member number/phone + password) to view their points and redeem badges/rewards from their own purchases, rather than everything being handled by admin; admin only creates the account and can reset the password. Confidence: 0.7
 to restore anything corrupted. Confidence: 0.9
- When checking whether a CDN is reachable from the dev machine, `Invoke-WebRequest`'s default user-agent gets 403 from jsdelivr while real browsers load fine — verify CDN URLs with a browser user-agent (`curl -H "User-Agent: Mozilla/5.0"`) before concluding the network blocks the CDN, and check whether page assets (CSS/JS) actually load when a page renders blank. Confidence: 0.85
- Wants context-aware UI on the POS kiosk: the calculator/numpad on the cashier's right panel should be hidden by default so the screen doesn't feel cramped ("ditutup aja biar gak sempit kali"), and only appear when the payment method is cash (tunai) — on-screen input tools show only when relevant to the active payment step. Confidence: 0.75
- Prefers the product-search/barcode-scan controls on the POS kiosk to sit at the top of the left column so they don't get pushed down or look cluttered ("semak") when the receipt/struk and cart grow — moved the "Cari Produk" card above the struk/keranjang to keep the layout tidy. Confidence: 0.6
- Finds large "card with header" containers for POS input controls distracting and non-aesthetic ("card cari produk itu mengganggu fokus karna nggak estetik") — prefers these controls rendered as a compact, low-visual-weight toolbar without a big card header, so they don't compete for attention with the transaction flow. Confidence: 0.7
- Wants the barcode-scan input and the product-name search input visually separated into distinct labeled rows ("pisahkan kolom barcode dengan pencarian") rather than combined into one block — each row gets its own label (Barcode / Cari) plus its own input and buttons. Confidence: 0.7
- When a state-dependent wrapper element is hidden with a class (e.g., `d-none` on the kiosk right panel while no shift is open), an AJAX fragment re-render that only replaces inner content will never remove that class — after "buka kas" the POS right panel stayed invisible because the fragment updated but `.kiosk-kanan` kept its `d-none`; state-driven re-renders must explicitly re-sync wrapper classes, not just innerHTML, so the right panel (ringkasan + pembayaran) appears as soon as cash is opened. Confidence: 0.8
- Before telling the user a bug is fixed, confirm the fix is actually committed, pushed, and served by the server — the right-panel fix sat uncommitted in the working tree, so the user kept running the old JS and still saw the bug; after pushing JS/PHP fixes, remind the user to hard-refresh (Ctrl+Shift+R) because the browser may keep serving the cached page. Confidence: 0.7
- Wants features to be genuinely mature/polished end-to-end or removed — after finding the "Buka Kas" card still visible after opening cash, demanded "tolong pastikan bener bener matang fitur nya kalok gak buang aja" (make sure the feature is really complete, otherwise just remove it); half-working features are worse than none, so incomplete flows must be finished properly or dropped rather than left in a broken state. Confidence: 0.6
- Flags large single-file implementations that mix concerns (kiosk view + AJAX handlers + hardware logic, e.g., a 1988-line transaksi.php) as maintainability and regression risks — wants such files split by responsibility into smaller external files (JS extracted per concern: POS UI/AJAX, hardware Web Serial, camera scanner) while preserving load order and verifying behavior is unchanged in a real browser. Confidence: 0.7
- Wants session state isolated per user role when different roles may share a browser/device — staff (kasir/admin) logout must only clear staff session keys (user_id, role, nama) and regenerate the session id, never wipe a concurrently-active member session (member_id); flagged this as a future blocker for a member self-service kiosk on the same device as the cashier. Confidence: 0.6
- Wants cashiers to have self-service access to their own transaction history (not admin-only) for cash-drawer reconciliation at shift close — since tutup kas requires counting the drawer, a cashier who finds money short should be able to review the transaction history themselves instead of guessing where the shortage came from ("pas kasir bingung uang nya ada yg kurang tinggal liat riwayat aja"). Confidence: 0.6
- Wants double-submission protection on the touchscreen POS: submit buttons must be set `disabled` BEFORE the AJAX `fetch()` for state-changing/sensitive actions (bayar, batalkan, buka_kas, tutup_kas, set_member, hapus_member) and re-enabled on error responses (danger/warning), failed requests (.catch), and after `gantiFragment()` if the old button wasn't replaced — the "Bayar" button was explicitly flagged as double-tap-prone on kiosk touchscreens. Confidence: 0.85
- Makes an explicit exception to the disable-during-AJAX rule for rapid-repeat actions like barcode 'scan' (and search forms): those buttons must NOT be disabled, because a cashier scans many barcodes in quick succession and disabling would slow the workflow instead of preventing a bug — only genuinely double-tap-sensitive, state-changing actions get disabled. Confidence: 0.85
- Expects client-side UX guards to sit on top of a server-side guard as defense-in-depth — even after the submit button is disabled client-side, the server must still reject a second payment on an already-emptied cart ("Keranjang masih kosong"), and that server guard must be locked with a regression test so a future refactor can't silently drop it. Confidence: 0.8
ice kiosk on the same device as the cashier. Confidence: 0.6
- Wants state-changing submissions (e.g., tutup kas) to give unambiguous success/failure feedback — after submitting, the modal/panel must visibly close or update and show a clear confirmation; a submit that leaves the UI looking unchanged made the user unsure whether it worked ("card nya nggak langsung ke tutup jadi bingung udh berhasil atau tidak"), which is treated as a UX bug to fix rather than just cosmetic. Confidence: 0.7
- Demands strictly phase-gated execution for large multi-part work, with explicit STOP points where no code may change: Fase 0 record a baseline (full e2e PASS count + current commit hash as rollback point) → Fase 1 audit-only (read the whole codebase, produce a written report grouped by security / money-flow bugs / code quality & architecture / test-coverage gaps / UI-UX, each finding tagged Kritis/Sedang/Kecil, and wait for the user's priority direction before touching any code) → Fase 2 fix issues one at a time → Fase 3 propose improvements only after fixes are done. Confidence: 0.95
- When auditing, re-verify previously-reported issues against the current code instead of assuming they were already fixed — the user explicitly enumerated past findings (debug files, .htaccess exposure, upload validation, member-login data leak, PDF export genuineness) and demanded each be re-checked ("verifikasi ulang apakah sudah kebenerin atau belum, jangan diasumsikan sudah fix"). Confidence: 0.85
- When genuinely uncertain between two approaches, ask the user rather than picking one — explicitly stated "Kalau ragu antara 2 pendekatan, tanya aku dulu daripada milih sendiri". Confidence: 0.9
- Improvement/upgrade proposals must be presented as a short list with a reason per item and left unimplemented until the user chooses — never guess the user's priorities ("jangan menebak prioritas aku sendiri"); this is the default unless the user explicitly delegates the choice. Confidence: 0.9
- Fixes must be scoped strictly to the issue at hand — explicitly told not to touch code unrelated to the issue being worked on ("Jangan sentuh kode yang nggak berhubungan sama isu yang lagi dikerjakan"). Confidence: 0.8
- When the same defect pattern is systemic across many files, treats it as ONE issue and fixes all instances in a single pass (e.g., the reused-`:cari` placeholder bug was fixed across 9 model files at once), rather than splitting into nine separate one-at-a-time cycles — the one-at-a-time rule governs distinct features/bugs, not each file of one shared root cause. Confidence: 0.6
- Before moving to the next phase of a multi-phase audit/fix effort, wants the FULL content of each skipped/deferred finding explained — not just short titles — covering what the problem is, why it was skipped/deferred, and the concrete risk if left as-is, with actual code shown as evidence (e.g., demanded the full S3/S5 write-up with the exact SQL snippet before allowing Fase 3, and explicitly gated it: "Jangan mulai Fase 3 sebelum aku baca penjelasan ini dan konfirmasi setuju"). Confidence: 0.85
- Never accepts "the app/feature needs it" as a reason to skip a security fix — when a feature depends on an exposed endpoint (e.g., hardware.config needed by hardware.js on the POS), the expected response is to find an alternative that is BOTH secure AND keeps the feature working (e.g., inject the config server-side and remove kasir access), not to leave the exposure in place; the user explicitly challenged the skip rationale ("kenapa solusinya 'dibutuhkan hardware.js di POS' itu dianggap cukup buat nge-skip... bukan malah jadi alasan buat dicari cara lain yang tetap aman TAPI hardware.js di POS tetap jalan"). Confidence: 0.8
- Security severity rule for dynamic SQL: any concatenated SQL part that originates from user input — even indirectly via an intermediate variable — MUST be classified Kritis (Critical) and fixed immediately, never deferred ("kalau ada bagian yang berasal dari input user meskipun via variabel perantara, itu HARUS masuk kategori Kritis, bukan ditunda"). Confidence: 0.9
- Money-flow/payment-processing code must be wrapped in try/catch so a runtime error (e.g., stock changed mid-payment) always releases the in-flight lock (e.g., `bayar_lock`) and surfaces a friendly message (`pesanErrorRamah`) instead of leaving the session stuck in a "processing" state and dumping a raw exception. Confidence: 0.6
- After mutating session/cart state (e.g., clearing the cart post-payment), stale superglobal input that a re-render reads (e.g., `$_POST['jumlah_dibayar']`) must be cleared (`unset`) before rendering dependent output/fragments, so derived values like "kembalian"/change reflect the current (empty) state rather than the old payment amount. Confidence: 0.5
- Prefers mandatory/required-step flows (like "buka kas") rendered as a centered modal overlay over a full-screen blurred backdrop (`position: fixed; inset: 0` + `backdrop-filter: blur()`), with NO close button so it cannot be dismissed until the required input (e.g., modal awal) is filled and successfully submitted; the required input is autofocused and background autofocus (e.g., the barcode field) must not steal focus while the overlay is up, and on failure the overlay stays visible with focus returned to the input. Confidence: 0.7
- Values generous whitespace and breathing room in the UI — explicitly asked for more margins so the web looks "estetik penuh ruang dan tidak menempel terlalu dekat" (full of space, elements not stuck too close together); spacing should be increased consistently across layout (column padding, gaps between toolbar rows/labels/forms, grid gutters, card padding) rather than applied as ad-hoc one-off margins. Confidence: 0.8
- After an AJAX state-changing action (e.g., payment), every server-derived display on the page must stay in sync — not just the primary cart/receipt fragment: the "Produk Lain" stock grid kept showing stale stock (68) after a sale while the DB was already 67. Fix pattern: render the secondary data via its own fragment function (e.g., `renderFragmentProduk()`), include it in the AJAX response, and have JS replace that container — so secondary displays refresh alongside the main fragment. Confidence: 0.6
- A terse follow-up like "ada lagi gak?" after a fix means the user wants the assistant to keep proactively re-auditing its OWN recent change for cascading/leftover bugs (not just the one reported) and fix them in the same session — e.g., after changing the product grid's `g-2` to `g-3`, the assistant self-caught and fixed the responsive media query that still selected `.row.g-2`, without being told what to look for. Confidence: 0.6
- When renaming/changing a CSS utility class used by a selector, verify all other selectors that reference the old class — including responsive media queries — so layout rules don't silently stop applying; the fix kept the selector tolerant of both old and new classes (`.row.g-2` AND `.row.g-3`) rather than dropping the old one. Confidence: 0.5
- Prefers responsive layouts that stack/wrap on small screens instead of clipping: a toolbar whose button overflowed the right edge on mobile was fixed by a `max-width: 575.98px` media query that stacks rows vertically, lets inputs go full-width, and wraps buttons — so controls never get cut off at phone width. Confidence: 0.6
- Prefers accessibility/robustness baked into the CSS baseline: a global `@media (prefers-reduced-motion: reduce)` block that shortens animation/transition durations, `env(safe-area-inset-*)` padding for notched devices, logical properties (`border-inline-start`/`padding-inline-end`) over physical `left`/`right` for RTL-readiness, a visible `:focus-visible` outline on buttons, a ~44px minimum touch target for icon-only buttons, and form inputs ≥16px on mobile (≤640px) so iOS Safari doesn't auto-zoom on focus. Confidence: 0.7
- Follows the design workflow with strict report-vs-apply mode discipline: diagnostic modes (`deslop`, `checkup`, `review`) only write reports — both `.md` and `.html`, following the bundled `design/references/*.md` templates — and must NOT modify interface files, while apply modes (`finish`, `typeset`, `responsive`) edit real files and produce no reports; an explicit `/design checkup` is report-only even when fixes are known. Confidence: 0.7
- Prefers the system font stack over custom webfonts for product UI — won't declare font families that may fail to load and silently fall back; builds visual hierarchy with type-scale tokens (`--text-xs`…`--text-xl`) and line-height tokens rather than dramatic size jumps. Confidence: 0.6
- Treats "tech gradients" (linear-gradient brand ribbons on navbar/headers) and colored "accent rails" (`border-left` on alerts) as default-template smells, preferring flat tones and full-border + tinted-background status styling instead. Confidence: 0.6
- Prefers UI motion that explains behavior and animates only `transform`/`opacity` (never `width`/`height`/`margin`/layout properties): tactile button press via `.btn:active { transform: scale(0.98) }` with `.btn:hover { transform: translateY(-1px) }`, product cards lifting on hover, short opacity+translate keyframes for flash/struk reveals, and loading spinners rotating ~0.8s — all automatically disabled under `prefers-reduced-motion`. Confidence: 0.7
- State-changing submit buttons should show an honest in-flight state (`.is-loading` with animated dots/spinner and `pointer-events: none`) while the fetch is pending, not just a silent disable; disabled buttons get `opacity` + `cursor: not-allowed` + `transform: none` so their status is unambiguous. Confidence: 0.65
- Wants keyboard focus preserved across modal open/close: after a Bootstrap modal closes (`hidden.bs.modal`), focus returns to the trigger element, and the modal's primary input is autofocused on open. Confidence: 0.6
- Prefers defensive repopulation of Bootstrap-modal hidden inputs so a submit can never send a stale/empty id: on `show.bs.modal`, reset the hidden id/nama fields to empty first, then fill from `e.relatedTarget`; ALSO attach a document-level delegated `click` fallback on the trigger button (`[data-bs-target="#modal-void"]`) that writes `data-produk-id`/`data-produk-nama` into the hidden fields before Bootstrap opens the modal, guaranteeing the id is never blank even if the `show.bs.modal` event is missed (root-caused a "item tidak ada di keranjang" bug where `produk_id=0` was sent because the hidden id stayed empty/stale). Confidence: 0.7
- Captures proven project workflows as reusable project-scoped skills (`.commandcode/skills/<name>/SKILL.md` with name/description/argument-hint frontmatter) — e.g., `debug-kasir-minimarket` encoding the reproduce→diagnose→fix→verify flow; after creating one, a bare "jalankan" means run the skill's first step (e.g., e2e baseline). Confidence: 0.6
- Wants no full-page-reload UX across the whole app, not just the POS — frustrated that "semuanya harus refresh dulu baru stok berkurang"; the accepted approach converts admin CRUD (produk/kategori add & delete) to fetch + JSON so the DataTable/list updates in place with a stable URL, with the server returning JSON instead of a redirect when it detects `X-Requested-With: fetch`. Confidence: 0.7
- When the user reports stale UI ("harus refresh dulu baru berubah") after JS/CSS edits, proactively bump the version query string on asset includes (`?v=YYYYMMDD`) so browsers fetch the new files instead of serving cached copies — going beyond just telling the user to hard-refresh. Confidence: 0.6
- AJAX fragment render functions must return their own wrapper element WITH its id, because the JS swaps the container via `outerHTML` — if the function returns only the inner content, the wrapper id is dropped and subsequent updates break (the POS "Produk Lain" grid wrapper `#fragmen-produk` vanished after payment, so the stock grid stopped updating). Confidence: 0.6
- Wants transactions performed by cashiers to be recorded/attributed so they roll up into the admin account — admin must be able to see all cashier transactions, not just their own ("pastikan transaksi kasir masuk ke akun admin"). Confidence: 0.6
- Wants state-gated prompt cards (e.g., the "buka kas"/open-shift card) to appear only once and not re-display repeatedly when the user exits/leaves ("pastikan card buka shift nggak berulang ulang di tampilkan saat keluar") — the shift-open state should persist and suppress the prompt across navigation/logout rather than re-showing it each time. Confidence: 0.55
